{
  "name": "new-folder",
  "version": "1.0.0",
  "description": "",
  "main": "json-server --watch db.json --port 8080",
  "scripts": {
    "start": "json-server --watch db.json --port 8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.2"
  }
}
